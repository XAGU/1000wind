<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="top.kwind.rlfz.course.mapper.CourseMenuMapper">
  <resultMap id="BaseResultMap" type="top.kwind.rlfz.course.pojo.CourseMenu">
    <!--@mbg.generated-->
    <!--@Table course_menu-->
    <id column="menu_id" jdbcType="INTEGER" property="menuId" />
    <result column="menu_name" jdbcType="VARCHAR" property="menuName" />
  </resultMap>
  <sql id="Base_Column_List">
    <!--@mbg.generated-->
    menu_id, menu_name, course_id
  </sql>

  <select id="selectByAll" resultMap="BaseResultMap">
    select menu_id,
           menu_name
    from course_menu
    <where>
      <if test="menuId != null">
        and menu_id =#{menuId,jdbcType=INTEGER}
      </if>
      <if test="menuName != null and menuName != ''">
        and menu_name like concat('%',#{menuName,jdbcType=VARCHAR},'%')
      </if>
      <if test="course != null and course.courseId != null">
        and course_id = #{course.courseId,jdbcType=INTEGER}
      </if>
    </where>
  </select>

  <insert id="insertMenu" useGeneratedKeys="true" keyProperty="menuId">
    insert into course_menu
    <trim prefix="(" suffix=")" suffixOverrides=",">
      menu_name,
      course_id
    </trim>
    values
    <trim prefix="(" suffix=")" suffixOverrides=",">
      #{menuName,jdbcType=VARCHAR},
      #{course.courseId,jdbcType=INTEGER}
    </trim>
  </insert>

  <update id="updateMenu">
      update course_menu
      <set>
          <if test="menuName != null and menuName != ''">
              menu_name = #{menuName,jdbcType=VARCHAR},
          </if>
          <if test="course != null and course.courseId != null">
              course_id = #{course.courseId,jdbcType=INTEGER}
          </if>
      </set>
      where menu_id =#{menuId,jdbcType=INTEGER}
  </update>

  <delete id="deleteMenus">
      delete from course_menu where menu_id in
      <foreach collection="array" item="id" open="(" separator="," close=")">
          #{id,jdbcType=VARCHAR}
      </foreach>

  </delete>

    <select id="selectByCourseId" resultMap="BaseResultMap">
        select menu_id,
               menu_name
        from course_menu where course_id =#{id,jdbcType=INTEGER}
    </select>

</mapper>